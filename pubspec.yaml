name: flutter_application_4
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0" # Updated SDK constraint

dependencies:
  flutter:
    sdk: flutter
  barcode_scan2: ^4.1.0
  http: ^0.13.3
  shared_preferences: ^2.0.6 # Added shared_preferences dependency
  intl: ^0.17.0 # Added intl dependency
  smooth_page_indicator: ^1.1.0 # Added smooth_page_indicator dependency
  google_fonts: 4.0.4 # Added google_fonts dependency


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
